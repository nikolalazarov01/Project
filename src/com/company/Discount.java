package com.company;

public interface Discount {
    /**
     *
     * @return Returns the percentage discount for the type of guest that implements the interface
     */
    double getPercentageDiscount();
}
