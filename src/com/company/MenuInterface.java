package com.company;

public interface MenuInterface {
    /**
     * Function that displays given menu
     * @param type Type of the menu being displayed
     */
    void Display(MenuTypes type);
}
